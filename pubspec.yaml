name: auto_size_text_max
description: Flutter 升级 3.35.7 之后，对 auto_size_text 插件的升级（After Flutter upgrade 3.35.7, the upgrade of auto_size_text plugins.）。
version: 4.0.0
homepage: https://github.com/HanQiGod/animated_text_kit_plus

environment:
  sdk: '>=2.12.0-0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: '>=1.11.1 <3.0.0'
